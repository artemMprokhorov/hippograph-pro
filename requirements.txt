# Neural Memory Graph - Python Dependencies
# Pinned versions for embedding consistency

flask>=3.0.0,<3.1.0
flask-cors>=4.0.0,<5.0.0
flask-socketio>=5.3.0,<6.0.0
numpy>=1.24.0,<2.0.0
torch>=2.0.0,<3.0.0
transformers>=4.30.0,<5.0.0
sentence-transformers>=2.2.0

# Entity extraction with spaCy (multilingual)
# Models are downloaded in Dockerfile via: python -m spacy download
spacy>=3.7.0,<4.0.0

# ANN indexing for fast similarity search with incremental updates
hnswlib>=0.8.0

# Graph metrics (PageRank, community detection)
networkx>=3.0
scipy>=1.10.0

# GLiNER: zero-shot NER (primary entity extractor, Apache 2.0)
# NOTE: gliner v1/base = CC BY-NC 4.0, only use v2.1+ (Apache 2.0)
gliner>=0.2.25

# GLiNER2: unified relation extraction + NER (sleep_compute deep phase, Apache 2.0)
gliner2>=1.2.0
